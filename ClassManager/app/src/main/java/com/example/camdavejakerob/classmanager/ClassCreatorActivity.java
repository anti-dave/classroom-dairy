package com.example.camdavejakerob.classmanager;

import com.google.firebase.database.DataSnapshot;

/**
 * Created by Davey on 3/20/2018.
 */
/*
* This Activity allows the user to create and add themselves to a class
* */
public class ClassCreatorActivity {

    //query firebase DB for getComponentName()

    //database.updateListViewUserClasses(this,mListView,"u0");
    /*DataSnapshot classSnapshot: dataSnapshot.getChildren()){

        database.writeNewClass(
                String classId,
                name = classSnapshot.child(CLASS_NAME).getValue().toString(),
                ArrayList<String> daysOfClass,
                startTime = classSnapshot.child(TIME_START).getValue().toString(),
                endTime = classSnapshot.child(TIME_END).getValue().toString(),
                room = classSnapshot.child(ROOM).getValue().toString());*/
}
